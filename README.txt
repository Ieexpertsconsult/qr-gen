
How to setup the project?
Create a database and go to masterdb.php file and put database credential.

Build on PHP collect form image data n more, with dashboard and stores them in sql
and generate Quick Response(QR)code.

Build on PHP which collect form image data n more, stores the path of image and qr code
in sql database with image uploaded and qr code stored in project folder and a dashboard
to Edit and delete all information.

Web Page
1> A form to collect Company/Oganisation detail
2> A Dashboard to Edit and Delete all detail
3> A Edit page which generate new QR code and upload new image


Note:- Can use Chrome Web Browser etc, Microsoft Visual Studio Code Editor or Sublime 
Text etc, XAMPP Apache Server *--> phpMyAdmin (SQL database interface).
